{
    "buildCommand": "cd client/ecom && npm install && npm run build",
    "outputDirectory": "client/ecom/dist",
    "installCommand": "npm install --prefix client/ecom",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}